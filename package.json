{
  "name": "hrcrm-monolith",
  "version": "1.0.0",
  "description": "Unified root deployment package",
  "private": true,
  "scripts": {
    "install": "npm --prefix frontend install",
    "build": "npm --prefix frontend run build"
  }
}
